# +++++ Configuration +++++++++++++++++++++++++++++++++++++++++++++++++++++++++ 
# --- Makefile Setup ---#
.ONESHELL:
default: install
.PHONY: default help install uninstall status
# --- MPS Setup ---#
MPSUSER :=mps
PAC_PACKAGES := sudo wget base-devel 
# +++++ Help ++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ 
help:
	@echo ""
	@echo "Usage: "
	@echo ""
	@echo "  make TARGET"
	@echo ""
	@echo "      TARGETS:"
	@echo "         help      : Help menu"
	@echo "         install   : Install components"
	@echo "         uninstall : Uninstall components"
	@echo "         status    : Current status"
	@echo ""
	@echo "  Example: make install"
	@echo ""
# +++++ Installation Targets ++++++++++++++++++++++++++++++++++++++++++++++++++ 
install: 
	@echo "Yes" | pacman --noconfirm -Syy \
		&& echo "Yes" | pacman --noconfirm -Syu \
    	&& echo "Yes" | pacman --noconfirm -S $(PAC_PACKAGES)
	@useradd -m -d /home/$(MPSUSER) -s /bin/bash $(MPSUSER) 
	@adduser mps sudo
	@echo '$(MPSUSER):$(MPSUSER)' | chpasswd
	@echo "$(MPSUSER) ALL=(ALL) NOPASSWD: ALL" >> /etc/sudoers
uninstall: 
	@echo "No uninstaller"
status: 
	@echo "No status"

